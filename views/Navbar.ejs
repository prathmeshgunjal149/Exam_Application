<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Shiv.EXAM Navbar</title>

  <!-- Font Awesome for Icons -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
  
  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background-color: #f5f9ff;
    }

    .navbar {
      display: flex;
      justify-content: space-between;
      align-items: center;
      background: linear-gradient(90deg, #0052D4, #4364F7, #6FB1FC);
      padding: 18px 50px;
      position: sticky;
      top: 0;
      z-index: 1000;
      box-shadow: 0 6px 16px rgba(0, 0, 0, 0.1);
      animation: slideDown 0.6s ease;
    }

    .logo-text {
      font-size: 26px;
      color: #fff;
      font-weight: bold;
      text-decoration: none;
      letter-spacing: 1.5px;
      transition: transform 0.3s ease;
    }

    .logo-text:hover {
      transform: scale(1.05);
    }

    .nav-links {
      list-style: none;
      display: flex;
      align-items: center;
      gap: 28px;
      margin: 0;
      padding: 0;
    }

    .nav-links li {
      position: relative;
    }

    .nav-links a {
      color: #fff;
      text-decoration: none;
      font-weight: 600;
      font-size: 16px;
      padding: 8px 14px;
      border-radius: 8px;
      display: flex;
      align-items: center;
      gap: 8px;
      transition: background 0.3s ease, transform 0.3s ease;
    }

    .nav-links a i {
      font-size: 14px;
    }

    .nav-links a:hover {
      background: rgba(255, 255, 255, 0.2);
      transform: translateY(-2px);
    }

    .dropdown .dropbtn {
      cursor: pointer;
      display: flex;
      align-items: center;
      gap: 8px;
    }

    .arrow {
      font-size: 12px;
      margin-left: 4px;
    }

    .dropdown-menu {
      display: none;
      position: absolute;
      top: 42px;
      left: 0;
      background: #fff;
      min-width: 220px;
      border-radius: 10px;
      box-shadow: 0 10px 20px rgba(0,0,0,0.2);
      animation: fadeIn 0.3s ease-in-out;
      overflow: hidden;
    }

    .dropdown-menu li a {
      color: #333;
      padding: 12px 18px;
      display: block;
      font-weight: 500;
      transition: background 0.3s, color 0.3s;
    }

    .dropdown-menu li a:hover {
      background-color: #f0f0f0;
      color: #0052D4;
    }

    .dropdown:hover .dropdown-menu {
      display: block;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(-10px); }
      to { opacity: 1; transform: translateY(0); }
    }

    @keyframes slideDown {
      from { transform: translateY(-100%); opacity: 0; }
      to { transform: translateY(0); opacity: 1; }
    }
  </style>
</head>
<body>

  <nav class="navbar">
    <div class="logo">
      <a href="/" class="logo-text"><i class="fas fa-graduation-cap"></i> Shiv.EXAM</a>
    </div>

    <ul class="nav-links">
      <li><a href="/"><i class="fas fa-home"></i> Home</a></li>

      <!-- Login Dropdown -->
      <li class="dropdown">
        <a href="#" class="dropbtn"><i class="fas fa-sign-in-alt"></i> Login <span class="arrow">▼</span></a>
        <ul class="dropdown-menu">
          <li><a href="/admin/login"><i class="fas fa-user-shield"></i> Admin Login</a></li>
          <li><a href="/student/login"><i class="fas fa-user-graduate"></i> Student Login</a></li>
        </ul>
      </li>

      <li><a href="/register"><i class="fas fa-user-plus"></i> Register</a></li>
      <li><a href="/about"><i class="fas fa-info-circle"></i> About</a></li>
      <li><a href="/contact"><i class="fas fa-envelope"></i> Contact Us</a></li>
    </ul>
  </nav>

</body>
</html>
